# 数值复现总指南

## 目标

这份指南把当前工作区中的数值脚本和论文中的核心概念一一对应起来，帮助你把“看懂综述”推进到“亲手跑过关键图像和关键数量级”。

## 脚本与论文映射

### 1. `finite_string_modes.py`

对应：

- 第 1 轮
- 正常模、离散谱、封闭边界条件

输出：

- 前几个本征函数图
- 数值本征频率与解析结果对比

### 2. `compact_support_qnm_scan.py`

对应：

- 第 2 轮
- Laplace 变换、Green 函数、Wronskian、QNM 极点

输出：

- 紧支撑势图
- `log |W(s)|` 热图
- 候选极点位置

### 3. `time_domain_wave_evolution.py`

对应：

- 第 2 到 3 轮
- 初始 burst、ringdown、晚时间衰减

输出：

- 势图
- 观测点波形
- 半对数图

### 4. `schwarzschild_potentials.py`

对应：

- 第 4 轮
- Regge-Wheeler 与 Zerilli 势

输出：

- `V(r)` 对比
- `V(r_*)` 对比

### 5. `wkb_schwarzschild_qnm.py`

对应：

- 第 5 轮
- Schwarzschild 基模数量级、势峰近似、WKB

输出：

- 势峰位置
- 一阶 WKB 的 `M\omega`
- 与文献值对比

### 6. `tov_polytrope.py`

对应：

- 第 7 轮
- TOV 背景、质量半径关系

输出：

- `M-R` 曲线
- 代表性背景模型剖面

### 7. `ringdown_fit_demo.py`

对应：

- 第 12 轮
- 探测、噪声、参数回收

输出：

- 含噪波形
- 拟合曲线
- 回收参数误差

## 推荐执行顺序

```powershell
python scripts/finite_string_modes.py
python scripts/compact_support_qnm_scan.py --fast
python scripts/time_domain_wave_evolution.py
python scripts/schwarzschild_potentials.py
python scripts/wkb_schwarzschild_qnm.py
python scripts/tov_polytrope.py
python scripts/ringdown_fit_demo.py
```

## 对结果的正确期待

这些脚本分为两类：

- 教学脚本
  目标是展示结构和数量级，不追求论文级数值精度。
- 半定量脚本
  能给出合理数量级并与文献值做基础比较。

因此，最重要的检查点不是“最后一位小数是否一致”，而是：

- 频率尺度是否正确
- 图像结构是否正确
- 物理解释是否通顺

## 每次运行后建议记录

每个脚本跑完后，都建议补 3 行实验记录：

1. 你看到了什么图像结构。
2. 它对应论文里的哪一段物理。
3. 哪个参数变化最影响结果。

